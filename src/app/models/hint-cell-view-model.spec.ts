import { HintCellViewModel } from './hint-cell-view-model';

describe('HintCellViewModel', () => {
  it('should create an instance', () => {
    expect(new HintCellViewModel(null)).toBeTruthy();
  });
});
