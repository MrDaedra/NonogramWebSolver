import { NonogramViewModel } from './nonogram-view-model';

describe('NonogramViewModel', () => {
  it('should create an instance', () => {
    expect(new NonogramViewModel()).toBeTruthy();
  });
});
